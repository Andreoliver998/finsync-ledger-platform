import { colors } from './colors';
import { spacing, radius, elevation } from './spacing';
import { typography } from './typography';

export const theme = {
  colors,
  spacing,
  radius,
  elevation,
  typography
};

export { colors, spacing, radius, elevation, typography };
